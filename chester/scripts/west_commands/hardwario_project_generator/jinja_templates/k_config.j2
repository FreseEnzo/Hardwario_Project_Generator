#
# Copyright (c) 2023 HARDWARIO a.s.
#
# SPDX-License-Identifier: LicenseRef-HARDWARIO-5-Clause
#

menu "Application"

# {{ variant_name | upper }} Project ------------------------------------------------- #
{% for feature in data['features'] %}
{#- A1 ------------------------------------------- #} 
    {%- if feature == 'a1' %}
# {{ feature | upper }}  ---------------------------------------------------------------------- #
config SHIELD_CTR_A1
	bool "Enable SHIELD_CTR_A1"
	default y 

config CTR_A1
	default y

config GPIO
	default y

config GPIO_PCA953X
	default y

config I2C
	default y
    {% endif -%}

{#- B1 ------------------------------------------- #} 
    {%- if feature == 'b1' %}
# {{ feature | upper }}  ---------------------------------------------------------------------- #
config SHIELD_CTR_B1
	bool "Enable SHIELD_CTR_B1"
	default y 

config CTR_B1
	default y

config GPIO
	default y

config GPIO_PCA953X
	default y

config I2C
	default y
    {% endif -%}

{#- BAROMETER_TAG ------------------------------------------- #} 
    {%- if feature == 'barometer_tag' %}
# {{ feature | upper }}  ---------------------------------------------------------------------- #
config SHIELD_CTR_BAROMETER_TAG
	bool "Enable SHIELD_CTR_BAROMETER_TAG"
	default y 

config I2C
	default y

config SENSOR
	default y

config MPL3115A2
	default y
    {% endif -%}

{#- C1 ------------------------------------------- #} 
    {%- if feature == 'c1' %}
# {{ feature | upper }}  ---------------------------------------------------------------------- #
config SHIELD_CTR_C1
	bool "Enable SHIELD_CTR_C1"
	default y 

config GPIO
	default y

config GPIO_PCA953X
	default y

config I2C
	default y
    {% endif -%}

{#- C2 ------------------------------------------- #} 
    {%- if feature == 'c2' %}
# {{ feature | upper }}  ---------------------------------------------------------------------- #
config SHIELD_CTR_C2
	bool "Enable SHIELD_CTR_C2"
	default y 

config GPIO
	default y

config GPIO_PCA953X
	default y

config I2C
	default y
    {% endif -%}

{#- DS18B20 ---------------------------------------- #} 
    {%- if feature == 'ds18b20' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_DS18B20
	bool "Enable SHIELD_CTR_DS18B20"
	default y 

config CTR_DS18B20
	default y
    {% endif -%}

{#- GNSS ---------------------------------------- #} 
    {%- if feature == 'gnss' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_GNSS
	bool "Enable SHIELD_CTR_GNSS"
	default y 

config CTR_GNSS
	default y
    {% endif -%}

{#- K1 --------------------------------------------- #} 
	{%- if feature == 'k1' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_K1
	bool "Enable SHIELD_CTR_K1"
	default y 

config CTR_K1
	default y
    {% endif -%}

{#- LRW ------------------------------------------- #} 
    {%- if feature == 'lrw' %}
# {{ feature | upper }}  ---------------------------------------------------------------------- #
config SHIELD_CTR_LRW
	bool "Enable SHIELD_CTR_LRW"
	default y 

config CTR_LRW
	default y
    {% endif -%}

{#- LTE ------------------------------------------- #} 
    {%- if feature == 'lte' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_LTE
	bool "Enable SHIELD_CTR_LTE"
	default y 

config CTR_LTE
	default y
    {% endif -%}

{#- MACHINE_PROBE ------------------------------------------- #} 
    {%- if feature == 'machine_probe' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_MACHINE_PROBE
	bool "Enable SHIELD_CTR_MACHINE_PROBE"
	default y 

config CTR_MACHINE_PROBE
	default y
    {% endif -%}

{#- MB7066_A --------------------------------------- #} 
    {%- if feature == 'mb7066_a' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_A
	bool "Enable SHIELD_CTR_MB7066_A"
	default y 

config CTR_X0
	default y

config MB7066
	default y
    {% endif -%}

{#- MB7066_B ---------------------------------------- #} 
    {%- if feature == 'mb7066_b' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_MB7066_B
	bool "Enable SHIELD_CTR_MB7066_B"
	default y 

config CTR_X0
	default y

config MB7066
	default y
    {% endif -%}

{#- METEO_A --------------------------------------- #} 
    {%- if feature == 'meteo_a' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_A
	bool "Enable SHIELD_CTR_METEO_A"
	default y 

config CTR_METEO
	default y

config CTR_X0
	default y
    {% endif -%}

{#- METEO_B ---------------------------------------- #} 
    {%- if feature == 'METEO_B' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_METEO_B
	bool "Enable SHIELD_CTR_METEO_B"
	default y 

config CTR_METEO
	default y

config CTR_X0
	default y
    {% endif -%}

{#- RTD_A --------------------------------------- #} 
    {%- if feature == 'rtd_a' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_A
	bool "Enable SHIELD_CTR_RTD_A"
	default y 

config CTR_X3
	default y

config CTR_RTD
	default y
    {% endif -%}

{#- RTD_B ---------------------------------------- #} 
    {%- if feature == 'rtd_b' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_RTD_B
	bool "Enable SHIELD_CTR_RTD_B"
	default y 

config CTR_X3
	default y

config CTR_RTD
	default y
    {% endif -%}

{#- S1 --------------------------------------------- #} 
	{%- if feature == 's1' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_S1
	bool "Enable SHIELD_CTR_S1"
	default y 

config CTR_S1
	default y

config I2C
	default y
    {% endif -%}

{#- S2 --------------------------------------------- #} 
    {%- if feature == 's2' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_S2
	bool "Enable SHIELD_CTR_S2"
	default y 

config CTR_HYGRO
	default y
    {% endif -%}

{#- SOIL_SENSOR --------------------------------------------- #} 
    {%- if feature == 'soil_sensor' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_SOIL_SENSOR
	bool "Enable SHIELD_CTR_SOIL_SENSOR"
	default y 

config CTR_SOIL_SENSOR
	default y
	{% endif -%}	

{#- WEIGHT_PROBE --------------------------------------------- #} 
    {%- if feature == 'weight_probe' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_WEIGHT_PROBE
	bool "Enable SHIELD_CTR_WEIGHT_PROBE"
	default y 

config CTR_WEIGHT_PROBE
	default y
	{% endif -%}	
	
{#- X0A -------------------------------------------- #} 
    {%- if feature == 'x0_a' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X0_A
	bool "Enable SHIELD_CTR_X0_A"
	default y 

config CTR_X0
	default y
    {% endif -%}

{#- X0B -------------------------------------------- #} 
    {%- if feature == 'x0_b' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X0_B
	bool "Enable SHIELD_CTR_X0_B"
	default y 

config CTR_X0
	default y
    {% endif -%}

{#- X2A -------------------------------------------- #} 
    {%- if feature == 'x2_a' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X2_A
	bool "Enable SHIELD_CTR_X2_A"
	default y 

config GPIO
	default y

config I2C
	default y

config PM_DEVICE
	default y

config SERIAL
	default y

config UART_SC16IS7XX
	default y
    {% endif -%}

{#- X2B -------------------------------------------- #} 
    {%- if feature == 'x2_b' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X2_B
	bool "Enable SHIELD_CTR_X2_B"
	default y 

config GPIO
	default y

config I2C
	default y

config PM_DEVICE
	default y

config SERIAL
	default y

config UART_SC16IS7XX
	default y
    {% endif -%}

{#- X2_MODBUS_A -------------------------------------- #} 
    {%- if feature == 'x2_modbus_a' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X2_MODBUS_A
	bool "Enable SHIELD_X2_MODBUS_A"
	default y

config X2_MODBUS_A
	default y

config GPIO
	default y

config I2C
	default y

config MODBUS
	default y

config MODBUS_BUFFER_SIZE
	default 64

config PM_DEVICE
	default y

config SERIAL
	default y

config UART_0_INTERRUPT_DRIVEN
	default n

config UART_ASYNC_API
	default y

config UART_INTERRUPT_DRIVEN
	default y

config UART_LINE_CTRL
	default n

config UART_SC16IS7XX
	default y

config I2C
	default y  
{% endif -%}

{#- X2_MODBUS_B ---------------------------------------- #} 
    {%- if feature == 'x2_modbus_b' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X2_MODBUS_B
	bool "Enable SHIELD_X2_MODBUS_B"
	default y

config GPIO
	default y

config I2C
	default y

config MODBUS
	default y

config MODBUS_BUFFER_SIZE
	default 64

config PM_DEVICE
	default y

config SERIAL
	default y

config UART_0_INTERRUPT_DRIVEN
	default n

config UART_ASYNC_API
	default y

config UART_INTERRUPT_DRIVEN
	default y

config UART_LINE_CTRL
	default n

config UART_SC16IS7XX
	default y
{% endif -%}

{#- X3_A --------------------------------------------- #} 
    {%- if feature == 'x3_a' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_CTR_X3_A
	bool "Enable SHIELD_CTR_X3_A"
	default y 

config CTR_X3
	default y
    {% endif -%}

{#- X3_B --------------------------------------------- #} 
    {%- if feature == 'x3_b' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X3_B
	bool "Enable SHIELD_CTR_X3_B"
	default y 

config CTR_X3
	default y
    {% endif -%}

{#- X4_A --------------------------------------------- #} 
    {%- if feature == 'x4_a' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_CTR_X4_A
	bool "Enable SHIELD_CTR_X4_A"
	default y 

config CTR_X4
	default y
    {% endif -%}

{#- X4_B --------------------------------------------- #} 
    {%- if feature == 'x4_b' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X4_B
	bool "Enable SHIELD_CTR_X4_B"
	default y 

config CTR_X4
	default y
    {% endif -%}

{#- X7_A --------------------------------------------- #} 
    {%- if feature == 'x7_a' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_CTR_X7_A
	bool "Enable SHIELD_CTR_X7_A"
	default y 

config CTR_X7
	default y
    {% endif -%}

{#- X7_B --------------------------------------------- #} 
    {%- if feature == 'x7_b' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X7_B
	bool "Enable SHIELD_CTR_X7_B"
	default y 

config CTR_X7
	default y
    {% endif -%}

{#- X9_A --------------------------------------------- #} 
    {%- if feature == 'x9_a' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_CTR_X9_A
	bool "Enable SHIELD_CTR_X9_A"
	default y 

config CTR_X9
	default y
    {% endif -%}

{#- X9_B --------------------------------------------- #} 
    {%- if feature == 'x9_b' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X9_B
	bool "Enable SHIELD_CTR_X9_B"
	default y 

config CTR_X9
	default y
    {% endif -%}

{#- X10 --------------------------------------------- #} 
    {%- if feature == 'x10' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_X10
	bool "Enable SHIELD_CTR_X10"
	default y 

config CTR_X10
	default y
    {% endif -%}

{#- Z --------------------------------------------- #} 
    {%- if feature == 'z' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_CTR_Z
	bool "Enable SHIELD_CTR_Z"
	default y 

config CTR_Z
	default y

config I2C
	default y
    {% endif -%}

{#- PEOPLE_COUTER ------------------------------------ #} 
    {%- if feature == 'people_counter' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config SHIELD_PEOPLE_COUNTER
	bool "Enable SHIELD_PEOPLE_COUNTER"
	default y

config PEOPLE_COUNTER
	default y

config I2C
	default y  
{% endif -%}

{#- TAMPER ----------------------------------------- #} 
    {%- if feature == 'tamper' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config APP_TAMPER
	bool "Enable tamper functionality"
	default y 
	select CTR_EDGE
    {% endif -%}

{#- REPORT_JITTER ---------------------------------- #} 
    {%- if feature == 'report_jitter' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config APP_REPORT_JITTER
	bool "Add random time jitter to report transfers"
	default y 
	select ENTROPY_GENERATOR
    {% endif -%}   

{#- LAMBRECHT ---------------------------------------- #} 
    {%- if feature == 'lambrecht' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config APP_LAMBRECHT
	bool "Enable SHIELD_APP_LAMBRECHT"
	default y

{% endif -%}

{#- FLIP_MODE ---------------------------------------- #} 
	{%- if feature == 'flip_mode' %}
# {{ feature | upper }} ---------------------------------------------------------------------- #
config APP_FLIP_MODE
	bool "Enable SHIELD_APP_FLIP_MODE"
	default y
{% endif -%}
{%endfor%}
endmenu

menu "Zephyr Kernel"
source "Kconfig.zephyr"
endmenu
